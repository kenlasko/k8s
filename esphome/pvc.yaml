apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-esphome-pvc
  namespace: homeassist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 3Gi
