apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  app.kubernetes.io/name: longhorn-esphome-pvc
  namespace: esphome
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 3Gi
