apiVersion: v1
kind: Service
metadata:
  labels:
    app: vaultwarden2
  name: vaultwarden2-ui
  namespace: vaultwarden
spec:
  type: ClusterIP
  ports:
  - port: 8087
    protocol: TCP
    targetPort: 8087
    name: web
  selector:
    app: vaultwarden2
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: vaultwarden2
  name: vaultwarden2-ws
  namespace: vaultwarden2
spec:
  type: ClusterIP
  ports:
  - port: 3012
    protocol: TCP
    targetPort: 3012
    name: web
  selector:
    app: vaultwarden2
