apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-vaultwarden2-pvc
  namespace: vaultwarden2
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-value
  resources:
    requests:
      storage: 250Mi
