kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: vaultwarden2-ui
  namespace: vaultwarden2
spec:
  entryPoints: 
    - websecure
  routes:
  - match: Host(`vaultwarden2.ucdialplans.com`) 
    kind: Rule
    services:
    - name: vaultwarden2-ui
      port: 8087
    middlewares:
    - name: traefik-security
      namespace: traefik
  tls:
    secretName: traefik-wildcard-cert
    options:
      name: tls-options-default
      namespace: traefik
---
kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: vaultwarden2-ws
  namespace: vaultwarden2
spec:
  entryPoints: 
    - websecure
  routes:
  - match: Host(`vaultwarden2.ucdialplans.com`) && Path(`/notifications/hub`)
    kind: Rule
    services:
    - name: vaultwarden2-ws
      port: 3012
    middlewares:
    - name: traefik-security
      namespace: traefik
  tls:
    secretName: traefik-wildcard-cert
    options:
      name: tls-options-default
      namespace: traefik
