apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whoami-https-cert
spec:
  commonName: whoami.ucdialplans.com
  secretName: whoami-https-cert
  dnsNames:
    - whoami.ucdialplans.com
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer