---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: glances-nuc1
  namespace: glances
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll
    keel.sh/match-tag: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: glances-nuc1
  template:
    metadata:
      labels:
        app: glances-nuc1
        name: glances-nuc1
    spec:
      nodeName: nuc1
      containers:
      - name: glances-nuc1
        image: nicolargo/glances:latest-full
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 100m
            memory: 160Mi
          limits:
            cpu: 100m
            memory: 160Mi
        env:
        - name: GLANCES_OPT
          value: "-w -t 10"

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: glances-svc-nuc1
  name: glances-svc-nuc1
  namespace: glances
spec:
  type: ClusterIP
  ports:
  - port: 61208
    protocol: TCP
    targetPort: 61208
    name: web-61208-nuc1
  - port: 61209
    protocol: TCP
    targetPort: 61209
    name: web-61209-nuc1
  selector:
    app: glances-nuc1

---
kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: glances-nuc1
  namespace: glances
spec:
  entryPoints: 
    - websecure
  routes:
  - match: Host(`glances.ucdialplans.com`) && PathPrefix(`/nuc1`)
    kind: Rule
    services:
    - name: glances-svc-nuc1
      port: 61208
    middlewares:
    - name: traefik-security
      namespace: traefik
    - name: subdomain-regex-pathfix
      namespace: traefik
  tls:
    secretName: traefik-wildcard-cert
    options:
      name: tls-options-clientauth
      namespace: traefik

