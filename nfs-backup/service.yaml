---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nfs-server-svc
  name: nfs-server-svc
  namespace: nfs-server
spec:
  type: ClusterIP
  ports:
  - port: 2049
    protocol: TCP
    targetPort: 2049
    name: web
  selector:
    app: nfs-server
