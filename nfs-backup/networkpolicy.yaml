apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: nfs-server
  namespace: nfs-server
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: longhorn
  egress: []
