apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: esphome
  namespace: homeassist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 6Gi
