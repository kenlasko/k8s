apiVersion: v1
kind: Service
metadata:
  labels:
    app: donetick-service
  name: donetick-service
  namespace: homeassist
spec:
  type: ClusterIP
  ports:
  - port: 2021
    protocol: TCP
    targetPort: 2021
    name: web
  selector:
    app: donetick