apiVersion: v1
kind: Service
metadata:
  labels:
    app: grocy-service
  name: grocy-service
  namespace: homeassist
spec:
  type: ClusterIP
  ports:
  - port: 9283
    protocol: TCP
    targetPort: 80
    name: web
  selector:
    app: grocy