apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-homeassist-pvc
  namespace: homeassist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-value
  resources:
    requests:
      storage: 10Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-haconfig-pvc
  namespace: homeassist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-value
  resources:
    requests:
      storage: 5Mi

