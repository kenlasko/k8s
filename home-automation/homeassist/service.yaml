apiVersion: v1
kind: Service
metadata:
  labels:
    app: homeassist-service
  name: homeassist-service
  namespace: homeassist
spec:
  type: ClusterIP
  ports:
  - port: 8123
    protocol: TCP
    targetPort: 8123
    name: ha-web
  selector:
    app: homeassist