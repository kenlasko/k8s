apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zwaveadmin
  namespace: homeassist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 500Mi
