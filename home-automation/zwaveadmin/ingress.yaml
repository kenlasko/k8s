kind: IngressRoute
apiVersion: traefik.io/v1alpha1
metadata:
  name: zwaveadmin
  namespace: homeassist
  annotations:
    external-dns.alpha.kubernetes.io/target: k3s.ucdialplans.com
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints: 
  - websecure
  routes:
  - match: Host(`zwaveadmin.ucdialplans.com`) 
    kind: Rule
    services:
    - name: zwaveadmin-service
      port: 8091
    middlewares:
    - name: traefik-security
      namespace: traefik
  tls:
    options:
      name: tls-options-clientauth
      namespace: traefik