apiVersion: v1
kind: Pod
metadata:
  name: cloud-headlamp-egress-tunnel
  namespace: tailscale
  labels:
    app: cloud-headlamp-egress-tunnel
spec:
  containers:
  - name: cloud-headlamp-egress-tunnel
    image: alpine/socat
    args: ["tcp-listen:80,fork,reuseaddr", "tcp-connect:cloud-headlamp-egress:80"]
    ports:
    - containerPort: 80
  restartPolicy: Always
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: node-role.kubernetes.io/control-plane
            operator: DoesNotExist
          - key: kubernetes.io/arch
            operator: In
            values: 
            - 'amd64'

---
apiVersion: v1
kind: Service
metadata:
  name: cloud-headlamp-egress-tunnel
  namespace: tailscale
spec:
  selector:
    app: cloud-headlamp-egress-tunnel
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  labels:
    tailscale.com/proxy-class: "run-on-worker"
  annotations:
    tailscale.com/tailnet-fqdn: 'cloud-headlamp.tailb7050.ts.net'
  name: cloud-headlamp-egress
spec:
  externalName: cloud-headlamp-egress
  type: ExternalName

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cloud-headlamp
  namespace: tailscale
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "cloud-headlamp.ucdialplans.com"
  rules:
  - backendRefs:
    - name: cloud-headlamp-egress-tunnel
      namespace: tailscale
      kind: Service
      port: 80