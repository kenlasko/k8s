apiVersion: v1
kind: Service
metadata:
  labels:
    app: sftpgo
  name: sftpgo-ui
  namespace: sftpgo
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    name: web
  selector:
    app: sftpgo
