---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ghost
  namespace: ghost
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "ghost.ucdialplans.com"
  rules:
  - backendRefs:
    - name: ghost-http
      namespace: ghost
      kind: Service
      port: 2368