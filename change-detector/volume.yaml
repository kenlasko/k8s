---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: change-detector-data
spec:
  storageClassName: "nfs"
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
  - hard
  - nfsvers=4.1
  nfs:
    path: /appdata/vol/change-detector
    server: 192.168.1.3
    readOnly: false

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: change-detector-data
  namespace: change-detector
spec:
  storageClassName: "nfs"
  volumeName: change-detector-data
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi