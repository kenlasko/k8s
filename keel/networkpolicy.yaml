apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: keel
spec:
  podSelector:
    matchLabels:
      app: keel
  ingress:
    - from:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              app.kubernetes.io/name: traefik
      ports:
        - port: 9300
  egress:
    - ports:
        - port: 80
        - port: 443