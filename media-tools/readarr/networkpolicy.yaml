apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: readarr
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              app.kubernetes.io/name: traefik
      ports:
        - port: 8787
  egress:
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              app: homeassist
      ports:
        - port: 8123
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 443
        - port: 80
    - to:
        - podSelector:
            matchLabels:
              app: sabnzbd
      ports:
        - port: 8080
    - to:
        - podSelector:
            matchLabels:
              app: transmission
      ports:
        - port: 9091
    - to:
        - podSelector:
            matchLabels:
              app: prowlarr
      ports:
        - port: 9696