apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-pvc
  namespace: media-tools
spec:
  volumeName: pv-readarr
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 500Mi

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-readarr
spec:
  storageClassName: longhorn-high-value
  type: PersistentVolumeClaim
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  claimRef:
    namespace: default
    name: readarr-pvc
