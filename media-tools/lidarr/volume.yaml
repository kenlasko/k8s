---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-lidarr
spec:
  storageClassName: "nfs"
  capacity:
    storage: 300Mi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
  - hard
  - nfsvers=4.1
  nfs:
    path: /appdata/vol/lidarr
    server: 192.168.1.3
    readOnly: false

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-lidarr
  namespace: media-tools
spec:
  storageClassName: "nfs"
  volumeName: nfs-lidarr
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 300Mi