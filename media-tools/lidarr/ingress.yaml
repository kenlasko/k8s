kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: lidarr
  namespace: media-tools
spec:
  entryPoints: 
  - websecure
  routes:
  - match: Host(`lidarr.ucdialplans.com`) 
    kind: Rule
    services:
    - name: lidarr-service
      port: 8686
    middlewares:
    - name: traefik-security
      namespace: traefik
  tls:
    secretName: traefik-wildcard-cert
    options:
      name: tls-options-clientauth
      namespace: traefik
