---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: lidarr-test
  namespace: media-tools
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "lidarr-test.ucdialplans.com"
  rules:
  - backendRefs:
    - name: lidarr-test-service
      namespace: media-tools
      kind: Service
      port: 8686