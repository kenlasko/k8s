apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: romm
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: romm=zurdi15/romm:latest
    argocd-image-updater.argoproj.io/romm.update-strategy: digest
    argocd.argoproj.io/sync-wave: "99"
spec:
  project: default
  source:
    repoURL: "git@github.com:kenlasko/k8s.git"
    path: media-tools/romm
    targetRevision: HEAD
    directory:
      exclude: "*_DISABLED.yaml"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: media-tools
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        pod-security.kubernetes.io/enforce: baseline
        pod-security.kubernetes.io/enforce-version: latest
    automated:
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
