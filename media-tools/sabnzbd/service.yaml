apiVersion: v1
kind: Service
metadata:
  labels:
    app: sabnzbd-service
  name: sabnzbd-service
  namespace: media-tools
spec:
  type: ClusterIP
  ports:
  - port: 8080
    protocol: TCP
    targetPort: http
    name: web
  selector:
    app: sabnzbd