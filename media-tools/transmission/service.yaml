apiVersion: v1
kind: Service
metadata:
  labels:
    app: transmission-service
  name: transmission-service
  namespace: media-tools
spec:
  type: ClusterIP
  ports:
  - port: 9091
    protocol: TCP
    targetPort: 9091
    name: web
  selector:
    app: transmission