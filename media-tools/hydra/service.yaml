apiVersion: v1
kind: Service
metadata:
  labels:
    app: hydra-service
  name: hydra-service
  namespace: media-tools
spec:
  type: ClusterIP
  ports:
  - port: 5076
    protocol: TCP
    targetPort: 5076
    name: web
  selector:
    app: hydra