apiVersion: v1
kind: Service
metadata:
  labels:
    app: hydra2-service
  name: hydra2-service
  namespace: media-test
spec:
  type: ClusterIP
  ports:
  - port: 5076
    protocol: TCP
    targetPort: 5076
    name: web
  selector:
    app: hydra2