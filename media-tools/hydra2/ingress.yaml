kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: hydra2
  namespace: media-test
spec:
  entryPoints: 
    - websecure
  routes:
  - match: Host(`hydra22.ucdialplans.com`) 
    kind: Rule
    services:
    - name: hydra2-service
      port: 5076
    middlewares:
    - name: traefik-security
      namespace: traefik
  tls:
    secretName: traefik-wildcard-cert
    options:
      name: tls-options-clientauth
      namespace: traefik
