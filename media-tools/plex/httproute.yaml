---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plex
  namespace: media-tools
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "plex.ucdialplans.com"
  rules:
  - backendRefs:
    - name: plex-service
      namespace: media-tools
      kind: Service
      port: 32400