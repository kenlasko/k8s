apiVersion: v1
kind: Service
metadata:
  labels:
    app: plex-service
  name: plex-service
  namespace: media-tools
spec:
  type: ClusterIP
  ports:
  - port: 32400
    protocol: TCP
    targetPort: 32400
    name: web
  selector:
    app: plex