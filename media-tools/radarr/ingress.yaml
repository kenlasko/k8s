kind: IngressRoute
apiVersion: traefik.containo.us/v1alpha1
metadata:
  name: radarr
  namespace: media-tools
spec:
  entryPoints: 
    - websecure
  routes:
  - match: Host(`radarr-test.ucdialplans.com`) 
    kind: Rule
    services:
    - name: radarr-service
      port: 7878
    middlewares:
    - name: traefik-security
      namespace: traefik
  tls:
    secretName: traefik-wildcard-cert
    options:
      name: tls-options-clientauth
      namespace: traefik
