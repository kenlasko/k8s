apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: lidarr-test-backup
  namespace: media-tools
spec:
  volumeSnapshotClassName: longhorn-backup
  source:
    persistentVolumeClaimName: pvc-lidarr-test