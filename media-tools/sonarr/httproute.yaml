---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
  namespace: media-tools
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "sonarr.ucdialplans.com"
  rules:
  - backendRefs:
    - name: sonarr-service
      namespace: media-tools
      kind: Service
      port: 8989