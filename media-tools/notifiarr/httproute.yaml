---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: notifiarr
  namespace: media-tools
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "notifiarr.ucdialplans.com"
  rules:
  - backendRefs:
    - name: notifiarr-service
      namespace: media-tools
      kind: Service
      port: 5454