apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-jackett2-pvc
  namespace: media-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-value
  resources:
    requests:
      storage: 100Mi
