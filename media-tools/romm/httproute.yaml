---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: romm
  namespace: media-tools
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "romm.ucdialplans.com"
  rules:
  - backendRefs:
    - name: romm-service
      namespace: media-tools
      kind: Service
      port: 8080