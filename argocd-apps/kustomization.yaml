resources:
  - keel.yaml

patches:
  - target:
      kind: Deployment
      name: keel
      namespace: kube-system
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: keel
        namespace: kube-system
      spec:
        replicas: 0