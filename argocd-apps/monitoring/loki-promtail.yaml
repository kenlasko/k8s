apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki-promtail
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "15"
spec:
  project: default
  sources:
  - repoURL: "https://grafana.github.io/helm-charts"
    chart: loki
    targetRevision: 6.30.1
    helm:
      valueFiles:
      - $values/manifests/monitoring/promstack/loki-values.yaml
  - repoURL: "https://grafana.github.io/helm-charts"
    chart: promtail
    targetRevision: 6.16.6
    helm:
      valueFiles:
      - $values/manifests/monitoring/promstack/promtail-values.yaml
  - repoURL: "git@github.com:kenlasko/k8s.git"
    ref: values
  destination:
    server: "https://kubernetes.default.svc"
    namespace: monitoring
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
