apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  app.kubernetes.io/name: longhorn-garmin-pvc
  app.kubernetes.io/instance: garmin-upload
  namespace: garmin-upload
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-value
  resources:
    requests:
      storage: 250Mi
