apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-prowlarr-config-pvc
  namespace: media-tools
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 600Mi

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pvc-prowlarr
spec:
  capacity:
    storage: 600Mi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeAttributes:
      numberOfReplicas: "3"
      staleReplicaTimeout: "30"
    volumeHandle: pvc-prowlarr
  storageClassName: longhorn