image:
  repository: linuxserver/maintainerr
  tag: 2.10.0
  registry: docker.io
securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false
  seccompProfile:
    type: RuntimeDefault
  capabilities:
    drop: ["ALL"]
    add:
    - "CHOWN"
    - "SETGID"
    - "SETUID"
    - "DAC_OVERRIDE"
resources:
  requests:
    cpu: 100m
    memory: 300Mi
  limits:
    # cpu: 500m
    memory: 400Mi
service:
  port: 80
persistentVolume:
  size: 600Mi
  mountPath: /opt/data