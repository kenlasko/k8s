apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-calibre-config-pvc
  namespace: media-tools
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5.5Gi

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pvc-calibre
spec:
  capacity:
    storage: 5.5Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeAttributes:
      numberOfReplicas: "3"
      staleReplicaTimeout: "30"
    volumeHandle: pvc-calibre
  storageClassName: longhorn