apiVersion: v1
kind: Service
metadata:
  labels:
    app: calibre-service
  name: calibre-service
  namespace: media-tools
spec:
  type: ClusterIP
  ports:
  - port: 8083
    protocol: TCP
    targetPort: http
    name: web
  selector:
    app: calibre