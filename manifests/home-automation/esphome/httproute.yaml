---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: esphome
  namespace: homeassist
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "esphome.ucdialplans.com"
  rules:
  - backendRefs:
    - name: esphome-service
      namespace: homeassist
      kind: Service
      port: 6052