---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-media
  namespace: media-apps
spec:
  storageClassName: nfs-csi
  volumeName: nfs-media
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 20Ti