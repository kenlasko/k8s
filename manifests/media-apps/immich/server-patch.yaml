apiVersion: apps/v1
kind: Deployment
metadata:
  name: immich-server
spec:
  strategy:
    type: Recreate
  template:
    spec:
      containers:
      - name: immich-server
        resources:
          requests:
            cpu: 3m
            memory: 400Mi
          limits:
            memory: 1.5Gi
