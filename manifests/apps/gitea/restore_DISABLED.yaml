---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: gitea
  namespace: gitea
spec:
  trigger:
    manual: restore-once
  restic:
    repository: nas01-s3-backup-gitea
    destinationPVC: nfs-gitea-data
    copyMethod: Direct
    cleanupTempPVC: true
    storageClassName: nfs-csi
    moverSecurityContext:
      runAsNonRoot: false
      runAsUser: 1000
      runAsGroup: 100
      seccompProfile:
        type: RuntimeDefault
    moverAffinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
          - matchExpressions:
            - key: node-role.kubernetes.io/control-plane
              operator: DoesNotExist
            - key: kubernetes.io/arch
              operator: In
              values: 
              - "amd64"