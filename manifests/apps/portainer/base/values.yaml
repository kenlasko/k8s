image:
  pullPolicy: Always
enterpriseEdition: 
  enabled: false
persistence:
  size: 1Gi
  storageClass: nfs-csi
  existingClaim: nfs-portainer
service:
  type: ClusterIP
nodeSelector:
  storage: longhorn
resources:
  requests:
    cpu: 1m
    memory: 45Mi
  limits:
    # cpu: 400m
    memory: 60Mi

    
