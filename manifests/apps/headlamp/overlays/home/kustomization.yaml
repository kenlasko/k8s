resources:
- ../../base
patches:
- path: httproute-patch.yaml
  target:
    kind: HTTPRoute
    name: headlamp
    namespace: headlamp
configMapGenerator:
- name: headlamp-values
  behavior: merge
  files:
  - values.yaml
generatorOptions:
  disableNameSuffixHash: true
