apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: infisical-managed-secrets
  namespace: redis
spec:
  externalSecretSpec:
    data:
      string: redis-example
      remoteRef:
        key: redis-password