apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: infisical-managed-secrets
  namespace: redis
spec:
  secretStoreRef:
    kind: SecretStore
    name: infisical
  target:
    name: password
  dataFrom:
  - find:
      path: redis