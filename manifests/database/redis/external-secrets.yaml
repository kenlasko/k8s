apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: infisical-managed-secrets
  namespace: redis
spec:
  secretStoreRef:
    kind: SecretStore
    name: infisical
    namespace: external-secrets
  target:
    name: password
  dataFrom:
  - find:
      path: redis