apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: infisical-managed-secrets
  namespace: redis
spec:
  externalSecretSpec:
    secretStoreRef:
      kind: SecretStore
      name: infisical
    target:
      name: password
    dataFrom:
    - find:
        path: redis