---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: redis-password
  namespace: redis
spec:
  refreshInterval: 1d
  secretStoreRef:
    name: akeyless
    kind: ClusterSecretStore
  target:
    name: redis-password
  dataFrom:
  - extract:
      key: /redis