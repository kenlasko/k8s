apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  labels:
    cnpg.io/cluster: standby
  name: standby
  namespace: cnpg
spec:
  namespaceSelector:
    matchNames:
    - cnpg
  podMetricsEndpoints:
  - port: metrics
  selector:
    matchLabels:
      cnpg.io/cluster: standby
      cnpg.io/podRole: instance
