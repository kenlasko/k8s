apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: manual-backup
  namespace: cnpg
spec:
  cluster:
    name: home
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io