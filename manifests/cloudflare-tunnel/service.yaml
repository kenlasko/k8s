apiVersion: v1
kind: Service
metadata:
  labels:
    app: ddns-updater
  name: ddns-updater-web
  namespace: cloudflare
spec:
  type: ClusterIP
  ports:
  - name: web
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: ddns-updater