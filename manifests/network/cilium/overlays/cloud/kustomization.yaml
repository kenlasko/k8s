apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# namespace: cilium
helmCharts:
- name: cilium
  repo: https://helm.cilium.io/
  version: 1.19.1
  releaseName: cilium
  namespace: cilium
  valuesFile: values.yaml
resources:
- ../../base
# - ip-pool.yaml
# - l2-announce.yaml