apiVersion: apps/v1
kind: Deployment
metadata:
  name: letsencrypt-cert-watcher
  namespace: cilium
spec:
  replicas: 1
  selector:
    matchLabels:
      app: letsencrypt-cert-watcher
  template:
    metadata:
      labels:
        app: letsencrypt-cert-watcher
    spec:
      containers:
      - name: letsencrypt-cert-watcher
        image: alpine:latest
        command: ["/bin/sh", "-c"]
        args:
          - |
            apk add --no-cache inotify-tools openssl openssh
            echo "Watching for secret updates..."
            while true; do
              inotifywait -e modify,create,delete /certs/tls.crt
              echo "Secret changed, running script..."
              /scripts/update-nas-cert.sh
            done
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 0
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - name: secret-vol
          mountPath: /certs
        - name: creds-vol
          mountPath: /creds
        - name: update-nas-cert-script
          mountPath: /scripts/update-nas-cert.sh
          subPath: update-nas-cert.sh
        - name: sshkey-vol
          mountPath: /scripts/nas-sshkey
          subPath: nas-sshkey
      volumes:
      - name: secret-vol
        secret:
          secretName: letsencrypt-wildcard-cert
      - name: creds-vol
        secret:
          secretName: nas-creds
      - name: sshkey-vol
        secret:
          secretName: nas-sshkey
          defaultMode: 0600
          items:
          - key: nas-sshkey
            path: nas-sshkey
      - name: update-nas-cert-script
        configMap:
          defaultMode: 0550
          items:
          - key: update-nas-cert.sh
            path: update-nas-cert.sh
          name: update-nas-cert-script
