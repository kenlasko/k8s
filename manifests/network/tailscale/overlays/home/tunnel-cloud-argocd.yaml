apiVersion: v1
kind: Pod
metadata:
  name: cloud-argocd-tunnel
  namespace: tailscale
  labels:
    app: cloud-argocd-tunnel
spec:
  containers:
  - name: cloud-argocd-tunnel
    image: alpine/socat:1.8.0.3
    imagePullPolicy: IfNotPresent
    args: ["tcp-listen:8410,fork,reuseaddr", "tcp-connect:cloud-argocd-link:8410"]
    ports:
    - containerPort: 8410
    resources:
      requests:
        cpu: 1m
        memory: 5Mi
      limits:
        # cpu: 50m
        memory: 10Mi
  restartPolicy: Always
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: node-role.kubernetes.io/control-plane
            operator: DoesNotExist
          - key: kubernetes.io/arch
            operator: In
            values: 
            - "amd64"

---
apiVersion: v1
kind: Service
metadata:
  name: cloud-argocd-tunnel
  namespace: tailscale
spec:
  selector:
    app: cloud-argocd-tunnel
  ports:
  - protocol: TCP
    port: 8410
    targetPort: 8410
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  annotations:
    tailscale.com/proxy-class: "run-on-worker"
    tailscale.com/tailnet-fqdn: "cloud-argocd.tailb7050.ts.net"
    tailscale.com/hostname: cloud-argocd-link
  name: cloud-argocd-link
spec:
  externalName: cloud-argocd-link
  type: ExternalName

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cloud-argocd
  namespace: tailscale
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: https
  hostnames:
  - "cloud-argocd.ucdialplans.com"
  rules:
  - backendRefs:
    - name: cloud-argocd-tunnel
      namespace: tailscale
      kind: Service
      port: 8410