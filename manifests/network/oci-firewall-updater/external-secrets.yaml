apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: private-api-key
  namespace: oci
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: akeyless
    kind: ClusterSecretStore
  target:
    name: private-api-key
  data:
  - secretKey: token
    remoteRef:
      key: /oci-private-key