---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: oci-private-key
  namespace: oci
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: akeyless
    kind: ClusterSecretStore
  target:
    name: oci-private-key
  data:
  - secretKey: oci-private-key
    remoteRef:
      key: oracle-cloud/oci-private-key

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: oci-config-file
  namespace: oci
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: akeyless
    kind: ClusterSecretStore
  target:
    name: oci-config-file
  data:
  - secretKey: oci-config-file
    remoteRef:
      key: oracle-cloud/oci-config-file