apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  # This name will be overridden by Kustomize
  name: BASE_APPS_PLACEHOLDER 
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=default"]
  generators:
  - git:
      repoURL: git@github.com:kenlasko/k8s.git
      revision: HEAD
      requeueAfterSeconds: 15
      files:
      # This path will be overridden by Kustomize
      - path: PATH_PLACEHOLDER
  syncPolicy:
    preserveResourcesOnDeletion: true
  template:
    metadata:
      name: '{{index .path.segments 2}}'
      annotations:
        argocd.argoproj.io/sync-wave: '{{ .syncWave | default "0" }}'
      labels:
        app.kubernetes.io/managed-by: argocd
    spec:
      project: default
      source:
        repoURL: git@github.com:kenlasko/k8s.git
        targetRevision: HEAD
        path: '{{.path.path}}'
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
  templatePatch: |
    spec:
      {{- if .ignoreDifferences }}
      ignoreDifferences:
      {{- .ignoreDifferences | toYaml | nindent 4 }}
      {{- end }}
      info:
      {{- range .info }}
        - name: "{{ .name }}"
          value: "{{ .value }}"
      {{- end }}
      syncPolicy:
        automated:
        {{- if ne .autoSync false }}
          prune: true
          selfHeal: true
        {{- else }}
          allowEmpty: true
          prune: false
          selfHeal: false
        {{- end }}
        syncOptions:
        {{- range .syncOptions }}
          - {{ . }}
        {{- end }}