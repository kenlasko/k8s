apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-ucdialplans
  namespace: traefik
spec:
  secretName: wildcard-ucdialplans-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.ucdialplans.com"