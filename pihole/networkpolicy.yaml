apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: pihole
  namespace: pihole
spec:
  podSelector: {}
  ingress:
    - from:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              app.kubernetes.io/name: traefik
    - from:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - port: 53
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
