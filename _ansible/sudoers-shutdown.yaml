---
- hosts: everything
  become: true
  become_user: root
  tasks:
  - name: Allow ken to sudo /sbin/shutdown
    community.general.sudoers:
      name: allow-shutdown
      state: present
      user: ken
      commands: /sbin/shutdown
