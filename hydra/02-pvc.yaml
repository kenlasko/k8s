apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-hydra-pvc
  namespace: hydra
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-value
  resources:
    requests:
      storage: 500Mi
