apiVersion: v1
kind: Service
metadata:
  labels:
    app: hydra-service
  name: hydra-service
  namespace: hydra
spec:
  type: ClusterIP
  ports:
  - port: 5076
    protocol: TCP
    targetPort: 6052
    name: web
  selector:
    app: hydra