apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: kustomize-csi-snapshotter-replicas
namespace: kube-system
patches:
  - target:
      kind: Deployment
      name: snapshot-controller
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
