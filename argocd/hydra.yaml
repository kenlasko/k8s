apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hydra
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: hydra=linuxserver/nzbhydra2:latest
    argocd-image-updater.argoproj.io/hydra.update-strategy: digest
spec:
  project: default
  source:
    repoURL: 'git@github.com:kenlasko/K3S.git'
    path: media-tools/hydra
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: media-tools
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
