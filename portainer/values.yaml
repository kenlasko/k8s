image:
  pullPolicy: Always
persistence:
  size: 500Mi
  storageClass: nfs-client
service:
  type: ClusterIP
nodeSelector:
  node-type: worker
resources:
  requests:
    cpu: 100m
    memory: 100Mi
  limits:
    cpu: 500m
    memory: 200Mi

    
