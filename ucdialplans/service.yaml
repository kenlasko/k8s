apiVersion: v1
kind: Service
metadata:
  labels:
    app: ucdialplans-service
  name: ucdialplans-service
  namespace: ucdialplans
spec:
  type: ClusterIP
  externalTrafficPolicy: Local
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    name: web
  selector:
    app: ucdialplans