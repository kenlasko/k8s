apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-zwaveadmin-pvc
  namespace: homeassist
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-high-value
  resources:
    requests:
      storage: 500Mi
