apiVersion: v1
kind: Service
metadata:
  labels:
    app: vaultwarden
  name: vaultwarden-ui
  namespace: vaultwarden
spec:
  type: ClusterIP
  ports:
  - port: 8087
    protocol: TCP
    targetPort: http
    name: web
  selector:
    app: vaultwarden
