apiVersion: v1
kind: Service
metadata:
  labels:
    app: owntracks
  name: owntracks-ui
  namespace: owntracks
spec:
  type: ClusterIP
  ports:
  - port: 8083
    protocol: TCP
    targetPort: 8083
    name: web
  selector:
    app: owntracks
