---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: kubeapi
  namespace: default
spec:
  parentRefs:
  - name: tls
    namespace: cilium
    sectionName: tls-passthrough
  hostnames:
  - "k3s.ucdialplans.com"
  rules:
  - backendRefs:
    - name: kubernetes
      namespace: default
      kind: Service
      port: 443